import React from 'react';


/*interface McsOptions {
	options: {
		title: string;
		mode: number;
	}
}*/

//export default function McsDialog({options}: McsOptions) {

export default McsWidget;
//ReactDOM.render(<McsWidget/>, document.getElementById('#mcs-widget'));
